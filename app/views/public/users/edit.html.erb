<%= form_with model: @user, data: {turbo: false} do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :profile_image, "プロフィール画像" %>
  <%= f.file_field :profile_image %>

  <%= f.submit "更新" %>

<% end %>